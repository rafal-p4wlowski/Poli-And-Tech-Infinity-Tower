{
  "name": "poli-and-tech-infinity-tower-functions",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@upstash/redis": "^1.20.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}